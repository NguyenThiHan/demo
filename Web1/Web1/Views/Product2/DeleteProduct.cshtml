@model Web1.Models.ProductIndexModel

@{
    ViewBag.Title = "DeleteProduct";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*<h2>DeleteProduct</h2>
    <h3>Are you sure you want to delete this ?</h3>

    <div>
        <h4>Product</h4>
        <hr />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(@Model => Model.product.IdProduct)
                </dt>
                <dd>
                    @Html.DisplayFor(Model => Model.product.IdProduct)
                </dd>
                <dt>
                    @Html.DisplayNameFor(@Model => Model.product.NameProduct)
                </dt>
                <dd>
                    @Html.DisplayFor(Model => Model.product.NameProduct)
                </dd>
                <dt>
                    @Html.DisplayNameFor(@Model => Model.product.CodeProduct)
                </dt>
                <dd>
                    @Html.DisplayFor(Model => Model.product.CodeProduct)
                </dd>
                <dt>
                    @Html.DisplayNameFor(@Model => Model.product.QualityProduct)
                </dt>
                <dd>
                    @Html.DisplayFor(Model => Model.product.QualityProduct)
                </dd>
            </dl>
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default"/>
                 @Html.ActionLink("Back to List","GetAllProduct")
            </div>
        }
    </div>*@

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")

    <script type="text/javascript">
        $(function () {
            // Handler for .ready() called.
            //$.get('/product2/getTest', {}, function (response) {
            //    console.log(response);
            //    $('#divAllProducts').html(response);
            //});

            $.delete('/product2/DeleteProduct', {}, function (response) {
                $('#divAddProduct').html(response);
            });

        });
    </script>
}
